我打算改框架为一遍探索覆盖全图的版本，大体分为两个探索层级，第一个是宏观gvd层级，第二个是微观Frontier层级。
宏观上，我希望维护当前gvd图的已探索情况，启发式选择gvd探索目标时，只考虑gvd图上已有的“节点”（分支点，转角点，探索到一半中断的edge点），选择离自己最近且周围unknown格最多的节点前往探索（简单贪心，后续再修改），探索过后相关边和顶点标记为已探索
微观上，我希望维护当前gvd图的已清空情况，如果当前探索节点为叶节点，或一个分支节点但其他分支均已探索过，切入greedy局部清空模式，先做矩形flood画一个范围，之后在范围内进行greedy的frontier清空，清空顺序采用简单聚类按大小/距离排序（最朴素Frontier实现，后续再优化），清空后切回gvd探索模式，该节点标记为已清空
在整个gvd图更新和遍历结束后，进入扫尾模式，全局收集剩下的frontier，逐个贪心地收掉

1. 优化gvd图表示，对近邻节点做聚类，图更整洁（当前已有1m内不能有多个节点的硬约束，衡量一下是保留该约束好还是单独写一个聚类逻辑好）
2. 找一下现成的TSP包。现在我希望动态对gvd图做全局规划：用最少回头路走所有节点。每次gvd图重建时进行重规划，规划时考虑已探索节点，不考虑已清空节点
3. 配套2，叶结点的定义改为：在TSP规划的路径中，再也不会第二次折返的节点。如果检测到：1. 机器人导航来到的节点是叶节点或2. 机器人刚刚探索过的节点在图更新后变为了叶节点，则执行Frontier清理。